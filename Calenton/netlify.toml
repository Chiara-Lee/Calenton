[build]
  command = "hugo --minify --baseURL $DEPLOY_PRIME_URL" # 构建命令
  publish = "public" # 发布路径

[context.production.environment]
  HUGO_VERSION = "0.128.0" # 这里可以指定Hugo版本

[context.deploy-preview]
  command = "hugo --gc --minify --baseURL $DEPLOY_PRIME_URL" # 预览部署时的构建命令

[context.branch-deploy]
  command = "hugo --gc --minify --baseURL $DEPLOY_PRIME_URL" # 分支部署时的构建命令
